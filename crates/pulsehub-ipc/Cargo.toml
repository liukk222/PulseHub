[package]
name = "pulsehub-ipc"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
pulsehub-core = { path = "../pulsehub-core" }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.150"

[lints]
workspace = true
